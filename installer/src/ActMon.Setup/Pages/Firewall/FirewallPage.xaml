<UserControl x:Class="ActMon.Setup.Pages.Firewall.FirewallPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="640">
    <StackPanel Margin="8">
        <TextBlock Text="Windows Firewall" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
        <TextBlock Foreground="#64748B" Margin="0,4,0,16" TextWrapping="Wrap"
                   Text="ActMon requires inbound access for these ports. Only these three rules were created — nothing else was touched."/>

        <TextBlock Text="Checking…" Visibility="{Binding IsChecking, Converter={StaticResource BoolToVis}}" Foreground="#64748B"/>

        <ItemsControl ItemsSource="{Binding Rows}" Visibility="{Binding IsChecking, Converter={StaticResource InverseBoolToVis}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0">
                                <Run Text="{Binding Label, Mode=OneWay}" FontWeight="SemiBold"/>
                                <Run Text=" — port "/>
                                <Run Text="{Binding Port, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Grid.Column="1" Text="✓ Allowed" Foreground="#16A34A"
                                       Visibility="{Binding Allowed, Converter={StaticResource BoolToVis}}"/>
                            <TextBlock Grid.Column="1" Text="✕ Not allowed" Foreground="#DC2626"
                                       Visibility="{Binding Allowed, Converter={StaticResource InverseBoolToVis}}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
