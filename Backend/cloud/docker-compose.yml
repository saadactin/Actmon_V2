version: "3.9"

services:
  cloud_api:
    build: .
    ports:
      - "8002:8002"
    env_file:
      - .env
    restart: unless-stopped
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8002
      --workers 2
